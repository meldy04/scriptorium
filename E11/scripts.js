function getBaseUrl() {
    // return the base URL of your deployed app (only the protocol + domain)
}

function getUTorId() {
    // return your UTorId
}

module.exports = {
    getBaseUrl,
    getUTorId,
};